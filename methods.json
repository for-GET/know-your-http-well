#!/usr/bin/env bash

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
$DIR/extract.coffee master/methods.md "^\`([\-A-Z]+)\` \| ?([^\|]*) \| ([^\|]+) \| ([^\|]+) \| \[([^\]]+)\]\(([^\)]+)\)" method description safe idempotent spec_title spec_href
