#!/usr/bin/env bash

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
$DIR/../extract.coffee $DIR/../../master/methods.md "^\`([\-A-Z]+)\` \| ?([^\|]*) \| ([^ ]) \| ([^ ]) \|? ?([^ ]?) ?\| \[([^\]]+)\]\(([^\)]+)\)" method description safe idempotent cacheable spec_title spec_href
