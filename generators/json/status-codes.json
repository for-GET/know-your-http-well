#!/usr/bin/env bash

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
$DIR/../extract.coffee $DIR/../../master/status-codes.md "^\`([0-9a-zA-Z]{3})\` \| ([^\|]+) \| ?([^\|]*) \| \[([^\]]+)\]\(([^\)]+)\)" code phrase description spec_title spec_href
