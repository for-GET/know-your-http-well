#!/usr/bin/env bash

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
$DIR/../extract.coffee $DIR/../../master/headers.md "^\`([\-0-9a-zA-Z]+)\` \| ?([^\|]*) \| \[([^\]]+)\]\(([^\)]+)\)" header description spec_title spec_href
