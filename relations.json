#!/usr/bin/env bash

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
$DIR/extract.coffee master/relations.md "^\`([\-0-9a-zA-Z]+)\` \| ?([^\|]*) \| \[([^\]]+)\]\(([^\)]+)\)" relation description spec_title spec_href
